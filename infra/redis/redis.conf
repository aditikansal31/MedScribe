# Persist data to disk (append-only file) so sessions survive container restart
appendonly yes
appendfsync everysec

# Reasonable memory ceiling for dev; eviction policy for session/cache safety
maxmemory 256mb
maxmemory-policy allkeys-lru

# Log level
loglevel notice
